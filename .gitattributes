# Auto-detect text files and normalize line endings to LF in the repo
* text=auto eol=lf

# Shell scripts must use LF (critical for Docker containers)
*.sh text eol=lf

# Explicitly declare Windows-only files to use CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
